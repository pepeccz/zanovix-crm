name: zanovix-crm

services:
  # PostgreSQL database
  postgres:
    image: postgres:15-alpine
    container_name: zanovix-crm-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-zanovix_crm}
      POSTGRES_USER: ${POSTGRES_USER:-zanovix}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - zanovix-crm-postgres-data:/var/lib/postgresql/data
    networks:
      - zanovix-crm-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-zanovix} -d ${POSTGRES_DB:-zanovix_crm}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # Redis cache (JWT blacklist, rate limiting)
  redis:
    image: redis:7-alpine
    container_name: zanovix-crm-redis
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD:-}
      --save 900 1
      --appendonly no
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    ports:
      - "127.0.0.1:6380:6379"
    volumes:
      - zanovix-crm-redis-data:/data
    networks:
      - zanovix-crm-network
    restart: always
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # API service: FastAPI
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    container_name: zanovix-crm-api
    env_file: .env
    ports:
      - "8010:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - zanovix-crm-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Admin Panel: Next.js
  admin-panel:
    build:
      context: .
      dockerfile: docker/Dockerfile.admin-panel
    container_name: zanovix-crm-admin-panel
    environment:
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:8010}
    ports:
      - "8011:3000"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - zanovix-crm-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://localhost:3000').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))\""]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

networks:
  zanovix-crm-network:
    name: zanovix-crm-network
    driver: bridge

volumes:
  zanovix-crm-postgres-data:
    name: zanovix-crm-postgres-data
  zanovix-crm-redis-data:
    name: zanovix-crm-redis-data
